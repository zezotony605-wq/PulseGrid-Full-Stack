# Benchmark results

Run the load-test profile and commit `summary.json` only after recording the host CPU, memory, Docker limits, test duration and git commit. Never publish a throughput or “zero errors” claim that was not reproduced by this run.
